{
  "name": "rag_add_tool",
  "description": "Add text to the RAG (Retrieval-Augmented Generation) system with automatic chunking and embedding generation.",
  "input_schema": {
    "type": "object",
    "properties": {
      "text": {
        "type": "string",
        "description": "The text content to add to the RAG system"
      },
      "source": {
        "type": "string",
        "description": "Optional identifier for the source (e.g., 'movie:12345', 'book:chapter1')",
        "nullable": true
      },
      "chunk_size": {
        "type": "integer",
        "description": "Number of words per chunk (default: 500)",
        "nullable": true,
        "default": 500
      }
    },
    "required": ["text"]
  },
  "output_schema": {
    "type": "string",
    "description": "JSON string containing success status, number of chunks added, and document IDs"
  }
}