{
  "name": "plex_ingest_batch",
  "description": "Ingest up to a specified number of unprocessed Plex media items into the RAG system.",
  "input_schema": {
    "type": "object",
    "properties": {
      "limit": {
        "type": "integer",
        "description": "Maximum number of Plex items to ingest in this batch (default: 5)",
        "nullable": true,
        "default": 5
      }
    },
    "required": []
  },
  "output_schema": {
    "type": "string",
    "description": "JSON string containing the list of ingested items and count of remaining items"
  }
}