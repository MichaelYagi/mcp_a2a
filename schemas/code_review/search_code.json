{
  "name": "search_code",
  "description": "Search for text or regex patterns in the codebase.\nReturns file paths, line numbers, and the matching text.",
  "input_schema": {
    "type": "object",
    "properties": {
      "query": {
        "type": "string",
        "description": "The string or regex to find (e.g., 'class Weather' or 'todo')."
      },
      "extension": {
        "type": [
          "string",
          "null"
        ],
        "description": "Filter by file type (e.g., 'py', 'js')."
      },
      "directory": {
        "type": [
          "string",
          "null"
        ],
        "default": ".",
        "description": "The folder to start searching from."
      }
    },
    "required": [
      "query"
    ]
  }
}